// Animações CSS

// let img = document.querySelector("img");
// let i = 0;
// setInterval(() => {
//   //   img.style.transform = `scale(${i-- / 1000})`;
//   //   img.style["-webkit-transform"] = `scale(${i++ / 1000})`;
//   //   img.style["-ms-transform"] = `scale(${i++ / 1000})`;
//   img.style["-moz-transform"] = `scale(${i++ / 1000})`;

//   img.style.transform += `skew(${(i -= 5)}deg)`;
//   img.style.transform += `rotate(${(i -= 5)}deg)`;
// }, 30);
